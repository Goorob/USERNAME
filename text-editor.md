# Text editor 
## Command line 
 is a user interface that is navigated by typing commands at prompt A command line, or terminal, is a text based interface to the system. You are able to enter commands by typing them on the keyboard and feedback will be given to you similarly as text.


For example:(ls) is which is typically a command is always the first thing you type.then we type anathor command so at the end we going to have command lines 

### Shell : 
This is a part of the operating system that defines how the terminal will behave and looks after running (or executing) commands for you
